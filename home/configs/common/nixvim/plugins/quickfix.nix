{ ... }:
{
  # Plugin: stevearc/quicker.nvim - enhanced quickfix UI
  # Not available as a built-in nixvim plugin
  # This plugin provides improved quickfix list with context lines, editable buffers, and better styling
  # To add manually, use extraPlugins in nixvim configuration
}
