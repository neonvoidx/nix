# Build outputs
result
result-*

# Old configuration backup
flake.nix.old

# Secrets - commit the template, but prevent committing it after it's been edited with real secrets
# After you add your real passwords, encrypt it with sops before committing

